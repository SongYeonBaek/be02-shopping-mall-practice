package com.example.demo.user.controller;
/*
import com.example.demo.model.request.PostCreateReq;
import com.example.demo.model.response.PostCreateUserRes;
import com.example.demo.service.UserService;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RequestMethod;
import org.springframework.web.bind.annotation.RestController;


@RestController
@RequestMapping("/user")
public class UserController {
    private UserService userService;

    public UserController(UserService userService) {
        this.userService = userService;
    }

    @RequestMapping(method = RequestMethod.POST, value = "/create")
    public ResponseEntity create(PostCreateReq request){
        PostCreateUserRes response = userService.create(request);
        return ResponseEntity.ok().body(response);
    }

}
*/
